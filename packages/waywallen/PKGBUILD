# Maintainer: LocalT0aster <90502400+LocalT0aster@users.noreply.github.com>

pkgname=waywallen
pkgver=0.3.8
pkgrel=1
pkgdesc='Wallpaper manager for Linux'
arch=('x86_64')
url='https://github.com/waywallen/waywallen'
license=('MIT')
depends=('ffmpeg' 'glibc' 'libglvnd' 'libpulse' 'libxkbcommon' 'mesa' 'qt6-base>=6.10' 'qt6-declarative>=6.10' 'qt6-grpc' 'qt6-websockets' 'sqlite' 'vulkan-icd-loader' 'zstd')
makedepends=('rust' 'clang>=22' 'cmake>=3.28' 'git' 'lld' 'llvm' 'ninja' 'pkgconf' 'protobuf' 'qt6-shadertools' 'qt6-tools' 'vulkan-headers')
optdepends=('waywallen-display: desktop integration clients')
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/v${pkgver}.tar.gz"
        'lito-v0.7.1-linux-x86_64.tar.gz::https://github.com/litocpp/lito/releases/download/v0.7.1/lito-v0.7.1-linux-x86_64.tar.gz')
sha256sums=('dd453063207a88bf577eb7854ddfd9e341bc8977b9debc8ffa09273ee9b0746f'
            '2987265fc8a0fcbe3bc84cad9501441349b25bfd64ef17dc024591caf079cb6a')

build() {
  export CC=clang
  export CXX=clang++
  cd "${pkgname}-${pkgver}"
  "${srcdir}/lito-v0.7.1-linux-x86_64/bin/lito" install \
    --no-config \
    --locked \
    --profile release \
    --prefix "${srcdir}/waywallen-stage/usr" \
    --config 'tools.cmake.search-path=["/usr"]'
}

package() {
  cp -a "${srcdir}/waywallen-stage/." "${pkgdir}/"
  install -Dm644 "${pkgname}-${pkgver}/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
